import {Button} from "@/components/ui/button.tsx";

export const DecideButton = () => {
    return (
        <Button className="h-16 cursor-pointer" type="submit">
            Decide
        </Button>
    );
};